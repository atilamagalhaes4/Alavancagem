import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.magalhaes.atila.alavancagem',
  appName: 'Alavancagem',
  webDir: 'www'
};

export default config;
